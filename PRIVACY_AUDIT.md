# GitHub 发布前：隐私/个人信息自检清单

本仓库包含备赛过程中的代码、数据说明、论文材料与过程记录。为了避免在 GitHub 上泄漏个人信息（队伍编号、姓名、邮箱、手机、本机路径等），建议在每次发布/推送前做一次自检。

## 1. 已处理（本仓库当前状态）

- 已清理项目内出现的本机绝对路径（如 `/Users/<用户名>/...`），统一改为仓库相对路径引用。
- 已将 LaTeX 模板中的队伍编号设为占位符（避免误提交真实 TC/队号）。
- 已将 Git 提交作者信息从个人邮箱切换为 `users.noreply.github.com`（避免提交历史里暴露个人邮箱）。

> 说明：如果你在其他分支/旧提交中仍包含个人信息，需要用同样方法清理并强制推送。

## 2. 一键扫描（推荐）

运行：

```bash
python3 u_tools/privacy_scan.py
```

它会检查：
- 仓库中**被 Git 跟踪的文件**（即可能被推送到 GitHub 的内容）里是否出现：
  - 本机路径（/Users、C:\Users 等）
  - 邮箱、手机号（粗略正则）
  - `tcn = ...` / Team Number 等队伍编号字段
  - 常见 token/密钥前缀（粗略）
- 同时检查：`git log` 的作者邮箱是否存在非 `users.noreply.github.com` 的情况。

## 3. 发布前人工复核（必做项）

1) 队伍编号/题号：
- 只允许保留占位符（例如 `tcn = 000000`、`<TBD>`）。
- 提交前不要把真实队号写进仓库（提交前临时替换即可，或使用本地不提交的配置文件）。

2) 人名/协作者信息：
- 过程记录/草稿里如果出现真实姓名、联系方式，建议：
  - 改成匿名代号（如 “队友A/队友B”），或
  - 仅保留结论与技术内容，删除身份信息。

3) 账号/密钥：
- 不要在 `.md/.py/.json` 中写任何 token、cookie、API key。
- 如必须写配置，使用 `*.example.*` 模板 + 本地私有配置（加入 `.gitignore`）。

## 4. 如果发现问题，怎么修

- **文件内容泄漏**：直接改文件并提交即可。
- **提交历史泄漏（最常见：作者邮箱/误提交密钥/队号）**：
  - 最干净的方式：重写历史并 `git push --force-with-lease`；
  - 或新建一个干净仓库重新提交（并删除旧仓库）。

## 5. 建议的工作流（最省心）

每次准备推送前：
1) `python3 u_tools/privacy_scan.py`
2) `git status --porcelain`
3) `git diff`（重点看：论文模板、README、配置文件、脚本中的路径/账号字段）

